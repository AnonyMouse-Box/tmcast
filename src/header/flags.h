// Included libraries
#include <stdbool.h>

// Local files
#include "hash.h"

// Definitions
#define NUM_FLAGS 1                     // Set number of flags

// Function prototypes
bool build_table(void);
bool write_node(flagTupleNode *ptrNode);
