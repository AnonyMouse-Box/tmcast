// Definitions
#define MAX 10000                       // Set upper bound
#define LEN(a) (sizeof(a) / sizeof(*a)) // Define array length

// Included libraries
#include <stdio.h>
#include <stdint.h>
#include <stdbool.h>
#include <limits.h>

// Local files
#include "flags.h"

// Function prototypes
